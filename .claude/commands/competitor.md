Run **one iteration** of the competitive-gap loop for Tarnoo.

**The objective is NOT feature parity.** It is that Tarnoo users plan and
discover excellent outdoor routes more effectively.

1. **Select one important route-planning workflow** (e.g. "plan a gravel loop from home", "turn a long route into daily stages", "find a good hike near a region").
2. **Establish how Tarnoo currently handles it** — verify from the repository (grep/read/route-test). This is the only source you can state as fact.
3. **Compare with competitor capability *only when reliable, dated evidence is available*.** Otherwise mark it `EXTERNAL RESEARCH REQUIRED` — never assert competitor pricing/features from memory.
4. **Identify the actual user-experience gap** (not a feature checkbox — a real friction in the workflow).
5. **Score** (1–5 each): user value · routing value · European-coverage value · differentiation · SEO value · effort.
6. **Classify P0–P3** (`P0` planner fundamental · `P1` major opportunity · `P2` useful · `P3` don't copy).
7. **Implement only P0/P1 opportunities when justified** — and only via the routing/SEO/europe loops as appropriate. One meaningful change per iteration.
8. **Do not copy social/community functionality by default** — build it only with strong evidence it improves route discovery/planning.
9. **Update `docs/claude/COMPETITOR_GAPS.md`** (matrix, verified facts + dates/sources, status).
10. **Update `docs/claude/ROADMAP.md`.**

Keep the "Tarnoo" column verified-from-repo and the competitor columns honest
(`UNKNOWN` / `EXTERNAL RESEARCH`) until confirmed. Lean into Tarnoo's real
differentiators (free, no account, all-Europe, route intelligence, GPX/FIT
interoperability) rather than chasing parity.
